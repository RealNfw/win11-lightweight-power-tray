#include <winuser.h>

// Phase 1: embed the application manifest only, so the CLI spike test
// harness gets correct OS capability detection (PerMonitorV2 DPI
// awareness, supportedOS list) without any application-compatibility
// shimming. IDI_APP is not referenced yet -- app.ico does not exist
// until Phase 4 packaging.
1 RT_MANIFEST "../app.manifest"
